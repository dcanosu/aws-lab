---
- name: Configure Server with Docker and CloudWatch
  hosts: all
  become: true
  roles:
    - docker

  tasks:
    - name: Show Web App URL
      debug:
        msg: "Your app is live at http://{{ ansible_host }}"

    - name: 🛰️ Install CloudWatch Agent
      yum:
        name: amazon-cloudwatch-agent
        state: present

    - name: 📝 Configure CloudWatch Agent JSON
      copy:
        dest: /opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
        content: |
          {
            "metrics": {
              "append_dimensions": {
                "AutoScalingGroupName": "{{ asg_name }}"
              },
              "aggregation_dimensions": [["AutoScalingGroupName"]],
              "metrics_collected": {
                "mem": {
                  "measurement": ["mem_used_percent"],
                  "metrics_collection_interval": 60
                }
              }
            }
          }

    - name: 🚀 Start and Enable CloudWatch Agent
      shell: |
        /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl -a stop
        /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl -a fetch-config -m ec2 -s -c file:/opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
